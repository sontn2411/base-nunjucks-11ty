<nav class="hidden lg:flex items-center gap-8 w-1/3">
  {% for item in menu %}
    <a
      class="text-xs font-semibold text-white/80 hover:text-primary transition-colors tracking-widest uppercase"
      href="{{ item.url }}"
      >{{ item.name }}</a
    >
  {% endfor %}
</nav>
