---
layout: layouts/base.njk
title: Quy trình khám - Medicare Clinic
permalink: /guide/
---

{% from "components/graphicVisual.njk" import graphicVisual %}
{% from "components/icons.njk" import icon %}

<main class="pt-20 pb-20 min-h-screen">
  <div class="topAbout_bg"></div>
  {{ graphicVisual('Quy trình', 'Hướng dẫn khách hàng', '/assets/images/banner3.png') }}

  <div class="container mx-auto px-6 py-12 max-w-6xl">
    <!-- Process Steps -->
    <div class="mb-20">
      <div class="text-center mb-16">
        <h2 class="text-3xl font-bold text-gray-900 mb-4">
          Quy trình khám chữa bệnh
        </h2>
        <p class="text-gray-600 max-w-2xl mx-auto">
          Quy trình đơn giản, nhanh chóng giúp quý khách tiết kiệm thời gian và
          an tâm điều trị.
        </p>
      </div>

      <div class="relative">
        <!-- Connecting Line (Desktop) -->
        <div
          class="hidden md:block absolute top-1/2 left-0 w-full h-1 bg-color-10 -z-10 -translate-y-1/2"
        ></div>

        <div class="grid grid-cols-1 md:grid-cols-5 gap-8">
          {% for item in guide.steps %}
            <div
              class="bg-white p-6 rounded-2xl shadow-sm border border-gray-100 flex flex-col items-center text-center relative hover:-translate-y-1 transition-transform duration-300"
            >
              <!-- Step Number Badge -->
              <div
                class="w-8 h-8 rounded-full bg-primary text-white font-bold flex items-center justify-center absolute -top-4 shadow-md"
              >
                {{ item.step }}
              </div>

              <!-- Icon -->
              <div
                class="w-16 h-16 rounded-full bg-color-10 text-primary flex items-center justify-center mb-4"
              >
                {% if item.icon == 'registration' %}
                  {{ icon('icn-guide', 'w-8 h-8') }}
                  {% elseif item.icon == 'examination' %}
                  {{ icon('doctors', 'w-8 h-8') }}
                  {% elseif item.icon == 'tests' %}
                  {{ icon('medical-endoscopy', 'w-8 h-8') }}
                  {% elseif item.icon == 'diagnosis' %}
                  {{ icon('icn-outpatient', 'w-8 h-8') }}
                  {% elseif item.icon == 'pharmacy' %}
                  {{ icon('medical-internal', 'w-8 h-8') }}
                {% endif %}
              </div>

              <h3 class="font-bold text-gray-900 mb-2">{{ item.title }}</h3>
              <p class="text-sm text-gray-600">{{ item.description }}</p>
            </div>
          {% endfor %}
        </div>
      </div>
    </div>

    <!-- Documents & Insurance Grid -->
    <div class="grid md:grid-cols-2 gap-12">
      <!-- Required Documents -->
      <div class="bg-white p-8 rounded-3xl shadow-sm border border-gray-100">
        <h3 class="text-xl font-bold text-primary mb-6 flex items-center gap-3">
          {{ icon('icn-guide', 'w-6 h-6') }} Giấy tờ cần thiết
        </h3>
        <ul class="space-y-4">
          {% for doc in guide.documents %}
            <li class="flex items-start gap-4 p-4 bg-gray-50 rounded-xl">
              <span
                class="mt-1 w-2 h-2 rounded-full bg-primary shrink-0"
              ></span>
              <div>
                <p class="font-bold text-gray-900">{{ doc.title }}</p>
                <p class="text-sm text-gray-600">{{ doc.note }}</p>
              </div>
            </li>
          {% endfor %}
        </ul>
      </div>

      <!-- Important Notes -->
      <div class="bg-primary/5 p-8 rounded-3xl border border-primary/10">
        <h3 class="text-xl font-bold text-primary mb-4 flex items-center gap-3">
          {{ icon('lightbulb', 'w-6 h-6') }}
          {{ guide.notes.title }}
        </h3>
        <p class="text-gray-700 mb-6">{{ guide.notes.description }}</p>

        <ul class="space-y-4">
          {% for item in guide.notes.list %}
            <li class="flex items-start gap-3">
              <span
                class="mt-1.5 w-1.5 h-1.5 rounded-full bg-primary shrink-0"
              ></span>
              <p class="text-gray-700">
                <span class="font-bold text-gray-900"
                  >{{ item.highlight }}:</span
                >
                {{ item.text | replace(item.highlight, '') | replace(':', '') | trim }}
              </p>
            </li>
          {% endfor %}
        </ul>

        <div
          class="mt-8 p-4 bg-white/60 rounded-xl border border-dashed border-primary/30 flex items-start gap-3"
        >
          <div class="text-primary mt-0.5">
            <svg
              xmlns="http://www.w3.org/2000/svg"
              class="w-5 h-5"
              fill="none"
              viewBox="0 0 24 24"
              stroke="currentColor"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"
              />
            </svg>
          </div>
          <p class="text-sm text-gray-600 italic">
            Nếu có bất kỳ thắc mắc nào, vui lòng liên hệ hotline
            <a
              href="tel:0123456789"
              class="font-bold text-primary hover:underline"
              >0123 456 789</a
            >
            để được hỗ trợ.
          </p>
        </div>
      </div>
    </div>
  </div>
  {% include "components/footer.njk" %}
</main>
